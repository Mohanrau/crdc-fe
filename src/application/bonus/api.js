import { url } from '../../global/api'

export const api = {
  'cwIncomeStatement': url.apiUrl + '/bonus-statement',
  'yearlyIncomeReport': url.apiUrl + '/yearly-income-report',
  'stockistCommission': url.apiUrl + '/stockist-commission',
  'cwBonusReport': url.apiUrl + '/cw-bonus-report'
}
