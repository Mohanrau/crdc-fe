import { url } from '../../global/api'

export const api = {
  'mailNotification': url.apiUrl + '/mail-notification',
  'taxInvoiceSummaryReport': url.apiUrl + '/tax-invoice-summary-report',
  'taxInvoiceByProductReport': url.apiUrl + '/tax-invoice-details-report',
  'saleProductReport': url.apiUrl + '/sale-product-report'
}
